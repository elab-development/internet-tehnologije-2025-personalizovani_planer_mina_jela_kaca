services:
  db:
    image: postgres:17
    container_name: planeri-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: planeri
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: planeri-app
    restart: always
    depends_on:
      - db
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_URL: "postgres://postgres:postgres@db:5432/planeri"
      API_URL: "http://localhost:3000"
      JWT_SECRET: "TAJNA_LOZINKA_ZA_JWT_TOKEN"
      JWT_EXPIRES: "7d"
    command: sh -c "npx drizzle-kit generate && npx drizzle-kit push && npm run db:seed && npm run dev"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - next_cache:/app/.next

volumes:
  pgdata:
  node_modules:
  next_cache: